# BiliQueue v0.1.16

本版本继续完善队列资格、礼物统计和横条动态显示，并加入大航海优先排队与应用内自动更新能力。

## 新增与优化

- 新增付费礼物排队、当前直播间粉丝牌排队和大航海排队资格，可组合使用并保持各自独立的判断逻辑。
- 新增大航海优先排队；等待队列按总督、提督、舰长、付费礼物优先和普通用户排列，同级用户保持原入队顺序。
- 识别舰长、提督和总督类别，在头像上显示对应大航海图标，并可在“队列调整与样式”中关闭横条图标。
- 横条可显示用户排队期间累计获得的电池数，礼物图标保留最后一次送出的礼物，免费礼物也会记录展示。
- 新增电池数字与图标大小设置，并优化队列序号、头像、用户名、礼物图标和累计电池数的边距约束。
- 手动添加用户统一使用蓝色星标头像，并支持选中后模拟礼物。
- 简易控制窗口缩小为 `420 × 560` 内容区域，全局快捷键可打开或关闭窗口，队列区域使用独立滚动条。
- 控制台复选框改为统一样式和四等分布局，顶部品牌栏新增“更新日志”和“检查更新”。
- 新增 Gitee 优先、GitHub 备用的更新检查；支持自动下载、校验、解压、替换并重启 Windows 发布版。
- 更新包改为下载并解压到主程序同级临时目录，准备完成后可选择立即更新或下次启动时更新，避免中断当前直播。
- 托盘菜单新增可勾选的“自动检查更新”，设置会保存到配置中；启动后延迟检查，并每 24 小时检查一次。

## 修复

- 修复分页滑动和淡入淡出模式在队列增减时闪回第一页的问题。
- 重构连续滚动轨道更新，保留现有节点和滚动位置，改善首尾衔接及增减用户时的闪烁。
- 修复双行队列在控制台增删、模拟排队和连续滚动时可能出现的第二行布局偏移。
- 修复队列管理拖拽到首位或末位时提示线缺失、拖放容易失败的问题。
- 修复说明区域收起且页面位于底部时，微调数字控件可能带动页面滚动的问题。

## 下载

- `BiliQueue-v0.1.16-windows.zip`：Windows AMD64 正式包。
- `BiliQueue-v0.1.16-windows.zip.sha256`：Windows 包 SHA-256 校验文件。
- `BiliQueue-v0.1.16-source.zip`：完整源码包。

## 升级提示

自动更新只替换主程序及随包资源，不会覆盖 `data` 和 `fonts`。Gitee 暂无可用 Release 时会自动改用 GitHub；原生简易控制窗口仍需要 Microsoft Edge WebView2 Runtime。

---
# BiliQueue v0.1.15

本版本在 v0.1.14 控制台与插件展示优化的基础上，补充 Windows 托盘快捷操作、全局快捷键和固定尺寸的原生简易控制窗口，并优化动态队列更新体验。

## 新增与优化

- 精简 Windows 托盘菜单，移除“打开横条”和“复制控制台地址”，新增“下一位”和带确认提醒的“清空队列”。
- 托盘保留“打开控制台”“打开简易控制页”和“复制浏览器源地址”等常用入口。
- 新增打开控制台、打开简易控制页、下一位和清空队列四组 Windows 全局快捷键，可在控制台录制、清除并查看注册状态。
- 简易控制页改为 `540 × 720` 的固定原生 WebView2 窗口，支持置顶与取消置顶，并会保留置顶状态。
- 简易控制窗口使用独立紧凑样式，队列区域独立滚动，礼物信息仅保留电池数，不影响网页控制台布局。
- 重复打开简易控制窗口时复用并激活已有窗口，关闭窗口后继续在后台保留以便快速再次打开。
- 未安装 WebView2 Runtime 时，可选择下载微软引导程序、使用默认浏览器打开简易控制页或取消。
- 清空队列、退出登录、导入配置和修改服务地址统一改为页面内确认弹窗，避免浏览器顶部提示被忽略。
- 新增 `-instance-id` 启动参数，便于开发测试时使用独立单实例命名空间。

## 修复

- 修复分页滑动和淡入淡出模式在新增或移除用户后短暂跳回第一页的问题；当前页仍有效时会继续停留在当前页。
- 修复简易控制窗口首次打开时可能短暂显示白屏的问题，加载期间改为深色等待界面。
- 修复快捷键配置在托盘创建、退出或重新注册并发发生时可能产生数据竞争的问题。

## 下载

- `BiliQueue-v0.1.15-windows.zip`：Windows AMD64 包
- `BiliQueue-v0.1.15-source.zip`：完整源码

## 升级提示

需要保留登录状态和现有设置时，请保留旧版的 `data` 文件夹。需要保留自定义字体时，请同时保留 `fonts` 文件夹。`data/auth.json` 含 B 站登录 Cookie，请勿分享。原生简易控制窗口需要 Microsoft Edge WebView2 Runtime，未安装时程序会提供下载或浏览器打开选项。

---
# BiliQueue v0.1.14

本版本为 v0.1.13 之后的控制台与插件展示优化版，重点重构控制台操作界面，并修复双行队列、连续滚动和礼物用户展示在动态更新时的布局问题。

## 新增与优化

- 控制台左右列改为等宽布局，主要卡片统一使用 18px 内边界和 45px 单行控件高度。
- 顶部预览区重新排列浏览器源地址、尺寸建议、操作按钮和连接状态。
- 队列管理改为拖拽排序；支持点选队列用户，并让模拟礼物插队作用于选中用户。
- 模拟礼物插队新增礼物价值输入框。
- 重构“文字编辑区”“排队与礼物规则”“大小与样式”“队列调整与样式”和“文字区域样式”布局。
- 文字编辑区和三个区域文字样式中的文字内容双向同步，并可在对应区域就地应用或放弃修改。
- 新增队列空态文字对齐，以及当前、队列和说明区域内部多行文字行间距控制。
- 控制台会在浏览器本地记住每个卡片的展开或收起状态。
- 页面右下角新增回到顶部按钮。
- 配置写入改为串行执行，降低自动保存与恢复默认、导入配置或应用文字相互覆盖的风险。
- 补充控制台和简易控制页异步操作的错误提示，并为复制地址增加兼容回退。

## 插件显示优化

- 调整礼物用户的序号、礼物图标、头像和用户名间距，使其统一受“队列区域头像文字间距”控制。
- 礼物用户高亮改为由左向右透明衰减的填充与描边，普通用户和礼物用户保持同一内容起点。
- 连续滚动模式改为与分页滑动、淡入淡出相同的固定列布局，单双行保持一致。
- 连续滚动更新队列时一次性生成完整循环轨道，并保留当前滚动位置，减少新增或移除用户时的瞬间位移。
- 优化分页滑动在单双行模式下的上下边缘渐隐位置。

## 修复

- 修复控制台移除或新增用户后，插件双行队列第二行可能布局异常的问题。
- 修复只有控制台模拟排队更新时第二行不能及时恢复布局的问题。
- 修复连续滚动超过同屏人数后，新增或移除用户会短暂跳回起点的问题。
- 修复真实礼物图标与模拟礼物占位图标垂直对齐不一致的问题。
- 修复文字区域颜色面板在页面底部关闭后可能导致页面向上滚动的问题。
- 修复说明区域文字行间距滑块没有作用于内部多行文字的问题。
- 修复文字区域下拉菜单可能出现白色背景和白色文字的问题。
- 修复队列管理高度会随右侧规则卡片收起或展开而改变的问题。

## 下载

- `BiliQueue-v0.1.14-windows.zip`：Windows AMD64 包
- `BiliQueue-v0.1.14-source.zip`：完整源码

## 升级提示

需要保留登录状态和现有设置时，请保留旧版的 `data` 文件夹。需要保留自定义字体时，请同时保留 `fonts` 文件夹。`data/auth.json` 含 B 站登录 Cookie，请勿分享。

---
# BiliQueue v0.1.13

本版本为 v0.1.12 之后的确认发布版，重点修复横条双行布局和分页滑动显示细节，并增加更灵活的区域开关与本地测试启动参数。

## 新增与优化

- “大小与样式”中新增“启用当前区域”和“启用说明区域”开关；关闭后对应区域不显示，也不占横条宽度。
- “统一高度”最小值改为 0，恢复默认值改为 50。
- 分页滑动改为上下边缘渐隐蒙版效果，保留向上滑动但不再使用整页淡入淡出。
- 双行分页滑动时，第二行使用独立渐隐蒙版，使渐隐位置贴合实际滑动行。
- 新增 `-fonts` 启动参数，便于本地测试时将数据目录和字体目录分别指向临时位置。

## 修复

- 修复横条从单行首次切换到双行时，第二行可能使用未稳定宽度计算布局而显示异常的问题。
- 修复分页滑动伪循环回到第一页时可能闪烁的问题。
- 修复分页滑动只有一页内容时仍套用渐隐蒙版的问题。
- 修正 Windows 剪贴板写入逻辑中的 unsafe.Pointer vet 提示。

## 下载

- `BiliQueue-v0.1.13-windows.zip`：Windows AMD64 包
- `BiliQueue-v0.1.13-source.zip`：完整源码

## 升级提示

需要保留登录状态和现有设置时，请保留旧版的 `data` 文件夹。需要保留自定义字体时，请同时保留 `fonts` 文件夹。`data/auth.json` 含 B 站登录 Cookie，请勿分享。

---
# BiliQueue v0.1.12

本版本为 v0.1.11 之后的确认发布版。v0.1.12-test1 至 v0.1.12-test25 为本地测试迭代，不单独发布 Release。

## 新增与优化

- 新增端口配置：端口写入配置，下次启动沿用。
- Windows 启动时端口被占用会弹出输入框，可改用新的本地端口或地址。
- 控制台增加本机服务地址修改入口，修改成功后自动跳转。
- 托盘菜单增加复制控制台地址、修改端口、打开简易控制页。
- 新增单实例运行，重复启动时提示已有 BiliQueue 正在运行。
- 新增 `/mini-control` 简易控制页，用于队列管理。
- 新增“是否开启排队”开关。关闭后弹幕排队和礼物插队不触发，右侧说明区域只显示说明内容。
- 新增主播清空队列弹幕指令，默认 `清空队列`，可在控制台自定义。
- 新增主播下一位弹幕指令，默认 `下一位`，可在控制台自定义。
- 登录状态存在且已保存直播间号时，启动后自动连接弹幕。
- 连续滚动改为固定格子长条逻辑，用户名省略长度按统一格子宽度计算。
- 当前区域“当前”标记改为浮在头像左上角的小卡片，并支持位置偏移。
- 修正双行队列第一行被拉开的问题。
- 新增每行列数、列间距和双行显示设置。
- 单行和双行分页改为上下切换，固定列布局按每行列数和列间距计算。
- 新增淡入淡出模式；分页滑动加入渐入渐隐，分页和淡入淡出支持停留时间、切换耗时设置。
- 控制台参数区支持折叠，条幅大小与样式、队列调整与样式、文字区域分为独立设置组。
- 控制台顶部标题只显示主版本号，例如 `BiliQueue v0.1.12`。
- 控制台文案统一为浏览器源描述，减少特定直播软件名称。
- 优化数值输入框样式，数值框右侧增加上下微调按钮。

## 修复

- 托盘退出改为先保存配置和队列，再立即关闭本地服务，减少退出后进程短暂残留。
- 端口占用和修改端口弹窗统一为自绘窗口，优化标题、提示、输入框布局和按钮。
- 启动时检测到端口被占用，点击“取消”后直接取消启动，不再弹出额外错误框。
- 检测到已有 BiliQueue 正在运行时，只显示提示，不再自动打开控制台页面。
- 修复托盘“修改端口”窗口可能未响应的问题，并避免重复打开多个修改端口窗口。
- 修复分页滑动切换后内容可能消失的问题。
- 修复切换滚动方式后部分效果需要刷新才生效的问题。
- 修复双行第二行超出队列区域后可能整行消失的问题。
- 修复“第二行每页最多人数”参数不生效的问题。
- 调整恢复默认按钮，使其尺寸和文字大小与收起 / 展开按钮一致。

## 下载

- `BiliQueue-v0.1.12-windows.zip`：Windows AMD64 包
- `BiliQueue-v0.1.12-source.zip`：完整源码

## 升级提示

需要保留登录状态和现有设置时，请保留旧版的 `data` 文件夹。需要保留自定义字体时，请同时保留 `fonts` 文件夹。`data/auth.json` 含 B 站登录 Cookie，请勿分享。

---

## BiliQueue v0.1.11

本版本为 v0.1.10 之后的确认发布版。v0.1.11-test1 至 v0.1.11-test6 为本地测试迭代，不单独发布 Release。

### 新增与优化

- Windows 版改为无控制台窗口程序。
- 支持直接双击 `BiliQueue-windows-amd64.exe` 静默启动到系统托盘。
- 将 BiliQueue 图标写入 exe 文件本身，资源管理器中会显示应用图标。
- 增加系统托盘右键菜单：打开控制台、打开横条、复制浏览器源地址、清空队列、打开数据文件夹、打开日志文件和退出。
- 增加本地日志文件：`data/logs/biliqueue.log`。
- 将横条文字内容编辑区移动到控制台前部。
- 编辑横条文字时不再实时同步，需点击“应用文字到横条”后才保存并同步到横条。
- 将启动脚本改为 `start-BiliQueue.cmd`，避免 Windows / zip / GitHub Actions 间的文件名编码问题。
- Windows 测试包说明文件统一为 `README.md`。

### 修复

- 修复托盘菜单第二次右键可能无法弹出的问题。
- 修复托盘菜单“退出”可能无法结束进程的问题。
- 修复 GUI 模式下日志可能没有输出的问题。
- 降低重复打开控制台或横条时页面偶发卡在加载中的概率。
- 统一程序数据目录为 exe 所在目录，直接双击 exe 与 cmd 启动读取同一份 `data`、`fonts`、`assets`。

### 下载

- `BiliQueue-v0.1.11-windows-test.zip`：Windows AMD64 测试包
- `BiliQueue-v0.1.11-source.zip`：完整源码

### 升级提示

需要保留登录状态和现有设置时，请保留旧版的 `data` 文件夹。需要保留自定义字体时，请同时保留 `fonts` 文件夹。`data/auth.json` 含 B 站登录 Cookie，请勿分享。




## v0.1.12-test25 本地测试

- 调整“排队与礼物规则”区域字段顺序：是否开启排队、最大人数、主播清空队列指令、主播下一位指令、加入指令、取消指令、礼物门槛。
- 分页滑动加入渐入渐隐效果，保留原有上下滑动，并在切换时让上一页淡出、下一页淡入。

## v0.1.12-test24 本地测试

- 连续滚动改为固定格子长条逻辑，按“每行列数”和“列间距”计算每个用户格子的宽度。
- 修复单行连续滚动时用户名省略长度不一致的问题。
- 修复启用双行后选择连续滚动仍进入分页滑动的问题；双行时第一行固定，第二行按固定格子连续滚动。
- 当前区域“当前”标记新增水平偏移和垂直偏移设置。

## v0.1.12-test12 本地测试

- 修正横条背景渐变为真正的二次蒙版效果，三个区域背景透明度保持独立。
- 减少队列操作和文字应用时的横条整块重绘闪烁。
- 增强主播 UID 识别，主播弹幕清空队列会读取更多 B 站房间字段，并在控制台显示主播 UID。

## v0.1.12-test20 本地测试

- 将文字描边改为双层文字结构，模拟 Photoshop 外部描边效果，避免描边侵入文字内部。
- 当前区域、队列区域、说明区域继续分别使用各自的描边粗细和描边颜色设置。
- 保留 v0.1.12-test16 的双行固定列对齐修复。

## v0.1.12-test16 本地测试

- 修复启用双行显示时第一行和第二行固定列布局有效宽度不一致的问题。
- 双行第一行移除旧版 flex 间距与外层 padding，和第二行分页使用一致的 page-copy / fixed-columns 宽度规则。
- 保留 v0.1.12-test15 的固定列布局、用户名省略号和同方向循环分页效果。
- 当前区域、队列区域、说明区域新增文字描边设置，可分别设置描边粗细和描边颜色。

## v0.1.12-test15 本地测试

- 队列分页布局改为内部左对齐的规整列布局。
- 单行分页、双行第一行、双行第二行分页统一按“每行显示最多人数”和队列区域实际宽度计算列数。
- 队列内容整体继续按照“短队列对齐”选项进行左侧或居中摆放。
